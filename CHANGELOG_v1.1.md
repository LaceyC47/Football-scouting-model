# v1.1 Fix

- Fixed `TypeError: Invalid value ... for dtype float64`.
- New fuzzy-merge columns now inherit the source column type.
- Text values such as source filenames can now be stored safely.
- Matching logic and thresholds are unchanged.
