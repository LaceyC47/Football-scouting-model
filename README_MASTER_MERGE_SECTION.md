## Master Player Merge Engine

Build the unified player-season database:

```cmd
python scripts\build_master_merge.py
```

Primary output:

```text
data/processed/master_player_dataset_v1.parquet
```
