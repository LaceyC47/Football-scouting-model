## Phase 1 — Parquet Inventory

Run:

```cmd
python scripts\run_inventory.py
```

The output is written to:

```text
reports/inventory/
```

This inventories every collected Parquet file before source mapping and merging.
